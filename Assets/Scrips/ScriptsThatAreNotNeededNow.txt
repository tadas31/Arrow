//If it is stopped in between the change
        if (spriteTime > 0 && time < (timeAmt + spriteTimeAmt) / 2 && time > (timeAmt - spriteTimeAmt) / 2 && stop)
        {
            ChangeSpriteToVisable();
        }